# Naming prompt parity report

The Version 3 prompt exposes five semantic blocks. Its normalized system and user message content preserves the frozen Version 2 empty, representative, and maximum-memory fixtures for both selected agents exactly; only local provenance metadata, family identity, and fingerprints changed.
