# The downloaded benchmark and all derived/population data now live in the
# repository's top-level data/hidden_bench/ (ignored by the root .gitignore).

# API-generated annotations, audits, and benchmark outputs
annotations/*.json
annotations/*.jsonl
results/

# Credentials and local caches
.cache/
caches/
secrets/
__pycache__/
*.py[cod]

# Operating-system metadata
.DS_Store
__MACOSX/
*:Zone.Identifier
