# Smoke analysis recipe.
#
# theoretical_reference must be none: this study sets
# game.options.epistemic_persistence: 0.74, and aggregation rejects any
# theory reference other than none for finite epistemic persistence
# (aggregation.py:3636). Without this file the study inherits the default
# single_affinity_revised reference and can run but never aggregate.
theoretical_reference: none
